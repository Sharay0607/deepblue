services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: deepblue
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - deepblue_data:/var/lib/postgresql/data

volumes:
  deepblue_data:
